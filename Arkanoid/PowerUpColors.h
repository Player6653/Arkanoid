#pragma once
#include <SFML/Graphics.hpp>

// Единый источник цветов бонусов.
namespace PowerUpColors {
    const sf::Color Fireball(230, 90, 40);
    const sf::Color FragileBlocks(90, 200, 220);
    const sf::Color WidePaddle(120, 220, 120);
    const sf::Color RemoveIndestructible(190, 90, 220);
}
