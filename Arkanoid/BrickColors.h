#pragma once
#include <SFML/Graphics.hpp>

// Единый источник цветов блоков.
namespace BrickColors {
    const sf::Color Normal(140, 145, 150);

    const sf::Color DurableFresh(205, 165, 90);
    const sf::Color DurableWorn(160, 120, 60);
    const sf::Color DurableCritical(110, 80, 40);

    const sf::Color Glass(195, 230, 255, 150);

    const sf::Color Indestructible(60, 55, 80);
}
